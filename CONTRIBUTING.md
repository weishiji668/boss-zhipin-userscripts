# 参与开发

先谢谢你有兴趣。这个项目的维护成本主要不在写新功能，而在「站点一改版，7 个脚本的选择器都可能失效」，
所以下面这些约定都是为了让改版修复变快。

## 本地跑起来

```bash
git clone https://github.com/<你的用户名>/boss-zhipin-userscripts.git
cd boss-zhipin-userscripts
npm install                # 只为跑测试；脚本本身零依赖
npm run dev                # 起本地安装服务（默认 http://127.0.0.1:8899/）
```

浏览器装好篡改猴后打开 `http://127.0.0.1:8899/`，点脚本名 → 弹窗点「安装」，之后改完代码点同名链接即可「更新」。

## 提交前必须过这三道门

```bash
npm run check        # = 语法 + 元数据 + 私有内容扫描
npm run changelog    # 改了脚本头部的 @description 就要重跑，否则 CI 会红
npm test             # 纯 Node 的单元测试，秒级
npm run test:all     # 含 Playwright 的浏览器用例（首次要 npx playwright install chromium）
```

## 目录约定

| 路径 | 说明 |
| --- | --- |
| `boss-*.user.js`（仓库根目录） | 7 个用户脚本本体。**必须留在根目录** —— `tests/*.cjs` 是按 `../boss-xxx.user.js` 读它们的 |
| `tests/` | Playwright + Node 测试。全部用 `page.route` mock 站点响应，**不打真实站点** |
| `tools/` | 开发与门禁工具：本地安装服务、语法/元数据/私有内容/更新日志检查 |
| `docs/` | 面向使用者的说明。README 只放「第一屏 + 导航」 |
| `images/` | 文档截图（本地模拟页面，不含真实账号信息） |

## 改脚本时的约定

1. **版本号**：语义化。改了行为就 `+1` 末位（1.3.0 → 1.3.1），加了功能进中位，破坏兼容进首位（0.x 阶段的脚本见脚本头部既有节奏）。
2. **更新日志写在 `@description` 里**：在原有文字**末尾追加**一段 `vX.Y.Z：修「……」`，说明"原来错在哪、现在怎么改"。
   不要删历史条目 —— 这份日志是用户判断要不要升级的唯一依据。写完跑 `npm run changelog` 同步 CHANGELOG.md。
3. **只改自己那一份**：7 个脚本通过本机键（`bw_*` / `bt_*` / `bc_*` / `bi_*` / `bd_*`）与 DOM 标记（`data-bwf*` / `data-bt-*` / `data-bc-*` / `data-bi-*`）协同。
   要新增跨脚本标记前，先在 issue 里说一声 —— 协同协议是全局契约，改一处要 7 个脚本一起看。
4. **写操作要克制**：任何新增的"替用户发请求 / 替用户点按钮"的能力，都必须满足：默认关闭、需要用户显式确认、有间隔与每日上限、有熔断。

## 站点改版了怎么办

1. 先跑一次页面，看控制台与面板提示。`boss-filter` 有自检：页面上有 ≥8 张卡片但脚本识别到 0 张时会明确提示 —— 那基本就是选择器过期。
2. 找出卡片容器与字段选择器（各脚本里搜 `job_detail`、`findCards`、`cardLinks`）。
3. 优先**加**选择器而不是**换**：老选择器往往在部分页面仍然有效，替换容易造成回退。
4. 改完把新选择器补进对应 `tests/*.cjs` 的 mock HTML 里，让测试能覆盖到，再提 PR。

## 不受理的改动

- 提高批量投递速度 / 去掉每日上限 / 绕过站点的验证码与风控
- 多账号、代理池、代运营类需求
- 把本机数据上传到任何服务端

## 提交信息

用中文或英文都行，但请写清"改了哪个脚本、解决什么"。例如：

```
boss-filter v1.3.1：修站点 9/24 改版后卡片选择器失效（.job-card-wrapper）
```
