# 安全与隐私

## 这个项目会碰什么、不会碰什么

| | 说明 |
| --- | --- |
| 数据存在哪 | 全部存在你自己的浏览器里（localStorage / 油猴存储）。项目不提供任何服务端，不上传任何数据。 |
| 会发哪些请求 | 公开的脚本都只在你手动点按钮时请求站点页面，没有周期性、后台请求。 |
| 需要你自备的东西 | 如果你要用 AI 相关功能，需要自己填自己的 API Key —— 项目里没有任何内置 key。 |
| 写操作 | `boss-insight` 的处置按钮（拉黑 / 删除聊天记录，不可逆），只在你点击时执行。唯一会替你发消息的「一键投递」不在本仓库分发。 |

## 请不要在 issue / PR 里贴这些东西

- 你的 `Zp_token`、`bst` cookie、任何 `Cookie:` 头的完整内容
- 带登录态的完整接口响应（含你的姓名、手机号、简历、HR 会话）
- 任何 API Key（截图前先遮住）

要贴请求或页面结构时，请把值替换成 `xxx`，只保留**字段名与结构**那样的问题描述就够了。

## 报告安全问题

如果发现的问题符合下面任一条，请**不要开公开 issue**，用 GitHub 的私密报告通道（Security → Report a vulnerability）：

- 脚本会把本机数据（岗位、聊天、Key）发到项目作者或第三方
- 脚本可以被页面上的第三方内容利用（例如伪造消息触发拉黑）
- 仓库里混进了不该公开的内容（本机路径、抓包、站点源码）

普通的功能失效（选择器过期、按钮点不动）走公开 issue 即可 —— 那类问题不是安全事件。

## 仓库自带的两道自动门禁

CI 每次都会跑 `tools/scan-private.cjs` 与 `tools/verify-headers.cjs`：前者防止把本机路径 / 抓包文件 /
站方源码 / 疑似真实 key 提交进来，后者校验脚本头部元数据。想绕过它们之前请先想清楚为什么。
